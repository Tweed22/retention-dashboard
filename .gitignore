# OS junk
.DS_Store
Thumbs.db

# Editor backups / swap files
*.swp
*.swo
*~
.idea/
.vscode/

# Environment / secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem
*.key

# Build outputs (Cloudflare Pages does its own; nothing to commit)
dist/
build/
.cache/
.wrangler/

# Sample data may contain student PII — keep it local only.
# Move sanitized fixtures to /test_fixtures/ if needed for tests.
sample_data/

# Node (if we ever add tooling)
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log
package-lock.json
yarn.lock
